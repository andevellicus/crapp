{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm: build",
            "type": "shell",
            "command": "cd client && npm run build",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
